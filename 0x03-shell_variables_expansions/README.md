# System Engineering DevOps

This repository contains shell scripts for the 0x03-shell_variables_expansions project.

## Scripts

- **0-alias**: Creates an alias `ls='rm *'` for demonstration purposes. Be careful when using it. Use `\ls` to bypass the alias.

